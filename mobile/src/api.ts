import axios from 'axios'; import * as SecureStore from 'expo-secure-store'; export const api=axios.create({baseURL:process.env.EXPO_PUBLIC_API_URL??'http://localhost:4000/api'}); api.interceptors.request.use(async c=>{const t=await SecureStore.getItemAsync('access_token');if(t)c.headers.Authorization=`Bearer ${t}`;return c}); export async function login(email:string,password:string){const r=await api.post('/auth/login',{email,password});await SecureStore.setItemAsync('access_token',r.data.token);return r.data.user} export async function logout(){try{await api.post('/auth/logout')}finally{await SecureStore.deleteItemAsync('access_token')}}

PORT=4000
DATABASE_URL=postgresql://postgres:Click123%23@127.0.0.1:5432/maintenance
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=1000d
CORS_ORIGINS=*
NODE_ENV=development
# Production: use NODE_ENV=production, a strong unique JWT_SECRET,
# an explicit comma-separated HTTPS CORS_ORIGINS value (never '*').
